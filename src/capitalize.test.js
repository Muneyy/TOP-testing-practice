import { capitalize } from './capitalize';

// eslint-disable-next-line no-undef
test('Capitalize function', () => {
    // eslint-disable-next-line no-undef
    expect(capitalize('chuwa')).toEqual('Chuwa');
});
